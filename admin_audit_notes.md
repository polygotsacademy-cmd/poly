# ملاحظات فحص لوحة تحكم يوسف

تاريخ الفحص: 2026-08-18
الرابط: https://polyglotsacademy-iota.vercel.app/
الحساب المستخدم: يوسف

## الدخول
- تسجيل الدخول نجح من النسخة المنشورة بحساب يوسف.
- ظهرت لوحة التحكم في القائمة بعد الدخول.

## لوحة إدارة الطلاب
- العنوان: إدارة الأكاديمية / لوحة التحكم.
- عدد الطلاب الظاهر: 41.
- يوجد زرا إضافة طالب وتحديث.
- يوجد تبويبان: إدارة الطلاب وإدارة المحتوى.
- يوجد بحث باسم المستخدم أو الاسم الظاهر.
- يوجد زر تعديل لكل طالب.
- الأعمدة الظاهرة: المستخدم، الاسم الظاهر، آخر ظهور، النقاط، الحالة، Polyglots AI، الأقسام، إجراء.
- ملاحظة واضحة: عرض المستخدمين في الجدول يبدو ملتصقًا بصريًا؛ النص يتكرر مثل فراوفراو و maryam_m_amaryam_m_a، مما قد يكون مشكلة CSS أو وجود username/displayName مكررَين بلا فصل واضح.
- الملاحظة الظاهرة تعرض كثيرًا من الطلاب «لم يظهر بعد»، وهو قد يكون صحيحًا لبعض الحسابات لكن يحتاج التحقق من دلالة آخر ظهور.
- الحساب يوسف غير ظاهر ضمن قائمة الطلاب، وهو مطابق للمطلوب.
- جميع الطلاب تقريبًا يظهرون 8 أقسام وAI مفعل؛ يحتاج اختبار شاشة التعديل لمعرفة إمكانية ضبطها فعليًا.

## اختبار لاحق
- لم يتم حفظ أو تعديل أي بيانات حتى الآن.
- الخطوة التالية: فتح زر تعديل لطالب، ثم اختبار تبويب إدارة المحتوى والأزرار والحقول دون إرسال النماذج.

## نموذج تعديل الطالب

يفتح النموذج كنافذة منبثقة ويحتوي على الاسم الظاهر، اسم الشات، النقاط، كلمة مرور جديدة، textarea لنتائج الاختبارات بصيغة JSON، السماح بالدخول، السماح باستخدام Polyglots AI، ثم مربعات للأقسام: الكلمات، القصص، الاختبارات، النطق، الألعاب، Polyglots AI، Poly chat، لوحة الشرف. توجد أزرار إلغاء وحفظ التعديلات.

نواقص/نقاط تحتاج التحقق أو التحسين: لا يوجد حقل اسم المستخدم لأنه معرّف ثابت، ولا يوجد حذف طالب، ولا يظهر داخل النموذج آخر ظهور أو سجل استخدام AI/chat، ولا يوجد شرح عملي لصيغة نتائج الاختبارات JSON. عبارة كلمة المرور «سيتم تفعيلها عبر نشر آمن لاحقًا» مربكة رغم أن النظام الحالي يعتمد على GitHub وإعادة النشر. يجب اختبار هل الحفظ فعلي أم يكتفي بواجهة.

## إدارة المحتوى: التصنيفات والكلمات

الواجهة تعرض إضافة تصنيف أو كلمة. نموذج التصنيف يحتوي اسم التصنيف ورمز/Emoji. نموذج الكلمة يحتوي التصنيف، الكلمة الألمانية، الترجمة العربية، الجمع، رابط صوت GitHub حالي، رفع صوت جديد، الصورة أو Emoji، ثم تحديد الجمهور: الجميع أو مستخدمون محددون، وقائمة مستخدمين، وزر حفظ الكلمة.

نواقص ظاهرة: لا توجد قائمة بالعناصر الموجودة، ولا تعديل أو حذف للتصنيفات والكلمات، ولا معاينة للصوت أو الصورة، ولا حالة رفع أو رابط الناتج بعد اختيار الملف، ولا حقول واضحة للعنوان/الوصف أو ترتيب العنصر. النص الإرشادي يقول إن Firestore هو التخزين بينما JSON نسخة احتياطية، وهذا يحتاج توضيحًا لأن الصوت يرفع إلى GitHub. قائمة المستخدمين المحددين تبدو كقائمة اختيار متعددة غير واضحة من العرض النصي، ويجب اختبار قابلية اختيار أكثر من مستخدم.

## إدارة القصص

نموذج القصة يحتوي عنوان القصة، المستوى، نصًا ألمانيًا، ترجمة عربية، رابط صوت GitHub حالي، رفع صوت جديد، صورة غلاف، رفع صورة جديدة، جمهور المحتوى، وقائمة المستخدمين، ثم حفظ القصة. لا تظهر قائمة قصص موجودة أو تعديل أو حذف أو معاينة للملفات. كما أن حقلي النص الألماني والترجمة العربية يظهران كـ textarea بلا وصف إرشادي واضح في العرض النصي.

## اختبار اليوم

النموذج يشرح محاولة واحدة لكل طالب وأن الدرجة حسب نسبة الإجابات الصحيحة من 1000 نقطة. الحقول هي تاريخ الاختبار، العنوان، والأسئلة بصيغة JSON، ثم الجمهور وقائمة المستخدمين، وزر الحفظ. الفحص البرمجي أكد أن التاريخ والعنوان والأسئلة مطلوبة، وأن قائمة المستخدمين `multiple` فعلًا.

نواقص مهمة: إدخال الأسئلة بصيغة JSON خام صعب على مستخدم غير تقني ولا توجد أداة مرئية لإضافة سؤال وخيارات وإجابة صحيحة. لا توجد قائمة بالاختبارات المنشورة أو تعديل أو حذف أو نسخ اختبار. لا يظهر حقل وقت بداية/نهاية أو حالة نشر، ولا توجد معاينة للاختبار أو تحقق مرئي من بنية JSON قبل الحفظ.

## ملاحظات تقنية إضافية

عند فحص DOM في تبويب اختبار اليوم، ظهرت عناصر غير مرتبطة مباشرة بالنموذج الحالي مثل «مسح لي فقط» و«مسح للجميع» و«إرسال» و«إغلاق»، وهي على الأرجح نافذة إعلان مخفية أو مكوّن آخر موجود في الصفحة. يجب التأكد من أن العناصر المخفية لا تتداخل مع الإرسال أو إمكانية الوصول.

## اختبار جمهور المحتوى

تم تبديل جمهور اختبار اليوم إلى «مستخدمون محددون» دون حفظ. ظهرت قائمة مستخدمين متعددة، لكن واجهة الاختيار غير واضحة بصريًا ولا تعرض عدد المختارين أو زرًا لتحديد الكل/إلغاء الكل. هذه نقطة مهمة عند التعامل مع 41 طالبًا.

## إضافة طالب

نموذج إضافة الطالب يوضح أن الحساب يصبح فعالًا بعد Commit وإعادة نشر Vercel. الحقول المطلوبة هي اسم المستخدم وكلمة المرور، بينما الاسم الظاهر اختياري والنقاط الابتدائية تبدأ من 0. يوجد خيار السماح بـ Polyglots AI ومربعات الأقسام الثمانية، وجميعها مفعلة افتراضيًا.

نواقص محتملة: لا يوجد اسم شات منفصل في نموذج الإنشاء رغم أن تعديل الطالب يحتوي اسم الشات، ولا يوجد اختيار حالة الحساب عند الإنشاء، ولا يوجد حقل payment status، ولا يوجد توضيح أن كلمة المرور لن تظهر لاحقًا. كما أن تفعيل كل الأقسام افتراضيًا قد يمنح الطالب صلاحيات أكثر من المطلوب ما لم ينتبه المشرف لإلغاء ما لا يحتاجه.
