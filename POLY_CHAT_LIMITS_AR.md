# حدود Poly chat الجديدة

تمت إضافة حدود يومية مركزية إلى المحادثات المباشرة بين المستخدمين. لم تعد هذه الحدود محفوظة في `localStorage`، بل تُحفظ داخل مستند المستخدم في Firestore في الحقل `chatUsage`.

| النوع | الحد اليومي |
|---|---:|
| الرسائل النصية | 20 رسالة |
| الرسائل المصورة | 4 صور |
| الرسائل الصوتية | 4 رسائل صوتية |
| مدة الرسالة الصوتية | 100 ثانية كحد أقصى |

عند إرسال رسالة أو صورة أو تسجيل صوتي، يستخدم الموقع Firestore Transaction لحجز الاستخدام أولًا. هذا يجعل العداد مشتركًا بين الأجهزة ويقلل احتمال تجاوز الحد عند استخدام الحساب في متصفحين في الوقت نفسه.

يتم إنشاء الحقل تلقائيًا عند أول استخدام:

```text
users/{username}/chatUsage
```

ولا يتم حذف أي رسائل أو مستخدمين أو بيانات حالية. كما تم إزالة نظام العدادات القديم الذي كان يستخدم مفاتيح `chat_media_limits_...` في `localStorage`.

## اختبار ما بعد النشر

بعد رفع النسخة إلى Vercel، سجّل الدخول بالحساب نفسه من جهازين. افتح قسم Poly chat وتأكد من ظهور عدادات الرسائل والصور والصوت. أرسل رسائل من الجهاز الأول، ثم افتح الحساب من الجهاز الثاني وتأكد من أن الأرقام محدثة.

جرّب تسجيلًا صوتيًا مدته أقل من 100 ثانية، ثم جرّب تسجيلًا يتجاوز 100 ثانية؛ يجب أن يتوقف التسجيل تلقائيًا عند 100 ثانية. إذا ظهر خطأ `Permission Denied` عند أول إرسال، فستحتاج قواعد Firestore إلى السماح للمستخدم الحالي بتحديث حقل `chatUsage` في مستنده. لا تغيّر قواعد Firestore عشوائيًا قبل مراجعتها.
