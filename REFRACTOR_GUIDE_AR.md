# دليل الملفات بعد تنظيم Polyglots الحالي

## كيف يعمل الموقع الآن؟

ملف `public/index.html` صار يحمل ملفات JavaScript وCSS بالترتيب نفسه الذي كان موجودًا داخل الملفين الكبيرين. لا يوجد تغيير مقصود في الوظائف أو الواجهة؛ التغيير هو أن كل قسم صار قريبًا من ملف مستقل.

> **قاعدة مهمة:** عند إرسال ملف للوكيل لتعديله، أرسل ملف القسم نفسه فقط مع شرح المطلوب. لا ترسل `app.js` أو `style.css`؛ هذان الآن مجرد ملفات تعريف تشير إلى أن المشروع انتقل إلى المجلدات الجديدة.

## ملفات JavaScript

| الملف | مسؤول عن | أرسله عندما تريد تعديل... |
|---|---|---|
| `public/modules/00-core.js` | تهيئة Firebase الحالية، تحميل JSON، حالة التطبيق، الدخول، القائمة، وتحميل الصفحة. | تسجيل الدخول، بيانات البداية، التنقل العام، البحث، أو إعدادات التطبيق المشتركة. |
| `public/modules/01-router.js` | اختيار الصفحة الحالية وتبديل المحتوى. | إضافة قسم جديد للقائمة أو تغيير طريقة التنقل بين الأقسام. |
| `public/modules/02-announcements.js` | إعلانات الإدارة واستقبالها. | إنشاء أو عرض أو إرسال إعلان. |
| `public/modules/03-messages-view.js` | قائمة جهات اتصال المحادثات. | شكل شاشة قائمة الرسائل أو المستخدمين الظاهرين فيها. |
| `public/modules/04-vocabulary.js` | التصنيفات، الكلمات، بطاقات الكلمة، البحث، والصوت. | الكلمات، التصنيفات، البحث، بطاقات المراجعة، أو تشغيل صوت الكلمة. |
| `public/modules/05-stories.js` | قائمة القصص، فتح القصة، وصوت القصة. | شكل القصص أو شاشة قراءة قصة. |
| `public/modules/06-quizzes.js` | كل أنماط الاختبار: بطاقات، اختيار، وتهجئة. | الأسئلة، التصحيح، النتيجة، أو أنماط الاختبار. |
| `public/modules/07-ai-chat.js` | واجهة Polyglots AI، اختيار الوضع، الصورة، الصوت، وإرسال الطلب. | واجهة AI أو المترجم أو استخدام الصورة/الميكروفون. |
| `public/modules/08-games-theme.js` | صفحة الألعاب، فتح اللعبة وإغلاقها، وتطبيق الستايل. | قسم الألعاب أو فتح لعبة داخل الموقع. |
| `public/modules/09-direct-messages.js` | نافذة المحادثة المباشرة، الوسائط، التسجيل الصوتي، والحذف. | الرسائل بين الطلاب، رفع صورة، تسجيل صوت، أو نافذة المحادثة. |
| `public/modules/10-mascots.js` | اختيار الشخصية وتحميلها. | الشخصيات أو صورة الحساب. |
| `public/modules/11-gamification.js` | النقاط، streak، ولوحة الشرف. | XP أو لوحة الشرف أو منطق التحفيز. |
| `public/modules/12-chat-dropdown.js` | إغلاق وفتح قائمة أوضاع Polyglots AI. | القائمة الصغيرة الخاصة بوضع AI فقط. |

## ملفات CSS

| الملف | مسؤول عن |
|---|---|
| `public/styles/00-foundation.css` | المتغيرات العامة، الخطوط، والخلفية الأساسية. |
| `public/styles/01-shell.css` | شاشة الدخول، الشريط العلوي، القائمة، المحتوى، والفوتر. |
| `public/styles/02-learning.css` | عناصر التعلم العامة والنطق. |
| `public/styles/03-stories-quizzes-games.css` | القصص والاختبارات والألعاب. |
| `public/styles/04-ai-chat.css` | النسخة الأساسية من واجهة AI. |
| `public/styles/05-responsive.css` | تعديلات الهاتف والتابلت والاختبارات المتجاوبة. |
| `public/styles/06-navigation.css` | القائمة الجانبية والإشعارات. |
| `public/styles/07-ai-chat-refinements.css` | تحسينات واجهة AI المتقدمة. |
| `public/styles/08-messages-modals.css` | الرسائل المباشرة والنوافذ المنبثقة. |
| `public/styles/09-chat-announcements.css` | تحسينات الدردشة الأخيرة والإعلانات والشريط العلوي. |

## عند طلب تعديل من الوكيل

أعطه: **المطلوب + ملف JavaScript المسؤول + ملف CSS المسؤول**. مثال جيد:

```text
عدّل بطاقة القصة لتظهر مستوى القصة ووقت القراءة.
الملفات: public/modules/05-stories.js وpublic/styles/03-stories-quizzes-games.css
لا تعدّل أي ملف آخر.
```

وإذا كان التعديل على البيانات فقط، مثل إضافة قصة أو كلمة، لا تحتاج ملف JavaScript جديد. ستعدل `stories.json` أو `words.json` الآن، ثم ستستخدم لوحة الإدارة عندما ننتقل إلى Supabase.

## ملفات يجب ألا نحذفها

لا تحذف `public/index.html`، ولا تغير ترتيب روابط CSS أو script tags داخله بلا اختبار؛ ترتيبها يحافظ على الشكل الحالي والدوال العالمية الحالية. احتفظ بالمجلد `_original_snapshot` في جهازك كنسخة رجوع، لكنه لا يحتاج أن يُنشر على Vercel.
