# تعليمات لوحة التحكم

تمت إضافة نواة لوحة تحكم داخل نفس الموقع وتظهر للمستخدم يوسف فقط.

## ما تم تنفيذه

لوحة التحكم تحتوي على تبويبين: إدارة الطلاب وإدارة المحتوى. إدارة الطلاب تعرض المستخدمين الموجودين في جلسة يوسف، باستثناء يوسف نفسه، وتقرأ بيانات الملف من `users/{username}` في Firestore. يمكن تعديل الاسم الظاهر، اسم الشات، النقاط، حالة الحساب، تفعيل Polyglots AI، الأقسام المتاحة، ونتائج الاختبارات بصيغة JSON. لا يتم عرض كلمة المرور القديمة؛ عند الحاجة يمكن تعيين كلمة مرور جديدة.

تعطيل `active` يمنع المستخدم من الاستمرار في الموقع عند فحص حالته، ويوقف تسجيل الدخول في الواجهة بعد التحقق من مستند Firestore. يُحدّث `lastSeen` بحد أقصى مرة كل خمس دقائق لكل مستخدم.

إدارة المحتوى تحتوي على نماذج للتصنيفات والكلمات والقصص واختبار اليوم. تُحفظ المجموعات في `contentCategories`, `contentWords`, `contentStories`, و`dailyQuizzes`. يمكن نشر المحتوى للجميع أو لمستخدمين محددين. اختبار اليوم يستخدم `dailyQuizAttempts/{username}_{YYYY-MM-DD}` لضمان محاولة واحدة، وتحسب الدرجة من نسبة الإجابات الصحيحة إلى 1000 نقطة.

المحتوى المنشور من `contentWords` و`contentStories` يُضاف إلى محتوى JSON القديم عند تسجيل الدخول، ولا يتم حذف ملفات JSON الحالية.

## متغيرات Vercel المطلوبة

أضف المتغيرات التالية في إعدادات المشروع:

```text
SESSION_SECRET=قيمة عشوائية طويلة لا تقل عن 32 حرفًا
PASSWORD_SALT=قيمة عشوائية طويلة وثابتة
GITHUB_TOKEN=Fine-grained token بصلاحية Contents: Read and write للمستودع فقط
GITHUB_OWNER=polygotsacademy-cmd
GITHUB_REPO=poly
GITHUB_BRANCH=main
```

لا تضع أيًا من هذه القيم داخل GitHub أو ملفات JavaScript في `public`.

## رفع الصوت والصور

رفع الملفات يتم عبر `/api/github-upload` من خلال الخادم، وليس مباشرة من المتصفح. الملفات تحفظ داخل `public/audio/words`, `public/audio/stories`, و`public/images/stories`. بعد Commit يبدأ Vercel نشرًا جديدًا، لذلك قد يحتاج الرابط إلى الانتظار حتى اكتمال النشر.

الحد الحالي للملف الواحد 8MB. استخدم ملفات MP3 قصيرة ومضغوطة للأصوات، وصور WebP أو JPEG صغيرة لتقليل حجم المستودع ووقت النشر.

## تغيير كلمة المرور

حقل كلمة المرور الجديدة يرسل الطلب إلى `/api/admin-password`. تُحفظ تجزئة كلمة المرور داخل `api/password-overrides.json` في Commit جديد، ثم تصبح فعالة بعد اكتمال نشر Vercel. كلمات المرور القديمة تبقى صالحة للحسابات التي لم تُغيّر.

## قواعد Firestore

قبل الاستخدام الفعلي، يجب أن تسمح قواعد Firestore بقراءة وكتابة مستندات المستخدم للموقع وفق الصلاحيات المناسبة، وأن تمنع الطالب من تعديل `active`, `aiEnabled`, `points`, `visibleSections`, و`testResults` الخاصة به. لا تفتح الكتابة العامة في الإنتاج.

## اختبار أولي

سجّل الدخول بيوسف، افتح لوحة التحكم، ثم جرّب تعديل طالب تجريبي. بعد ذلك أضف تصنيفًا أو كلمة، وسجّل خروج يوسف، ثم اختبر أن الطالب يرى المحتوى المنشور فقط. اختبر رفع ملف صغير بعد إعداد `GITHUB_TOKEN`, وتأكد من اكتمال نشر Vercel قبل محاولة تشغيله.
