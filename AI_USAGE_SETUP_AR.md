# تحديث حدود Polyglots AI

تم نقل عدادات الرسائل والصور والتسجيلات من `localStorage` إلى حقل `aiUsage` داخل مستند المستخدم في Firestore.

لا يتم حذف أي بيانات حالية. عند أول استخدام لـ Polyglots AI بعد التحديث، يقرأ الموقع الحقل إن وُجد، وإذا لم يوجد يبدأ عداد اليوم من الصفر ثم يحفظه في مستند المستخدم. لذلك سيظهر نفس العداد عند تسجيل الدخول من متصفح أو جهاز آخر بالحساب نفسه.

الحدود الحالية هي 20 رسالة نصية و3 صور و3 تسجيلات صوتية يوميًا لكل مستخدم. يتم تحديث العداد داخل Firestore باستخدام Transaction حتى لا يسمح فتح أكثر من جهاز بتجاوز الحد بسهولة بسبب التزامن.

تم أيضًا حذف مسار الحساب التجريبي `admin/admin`، وإبطال أي جلسة قديمة صادرة باسم `Polyglot`. حساب يوسف وباقي حسابات المستخدمين وكلمات مرورهم لم تتغير، ولم تُحذف أي بيانات.

بعد رفع النسخة إلى Vercel، اختبر قسم Polyglots AI من جهازين بالحساب نفسه. إذا ظهر خطأ Permission Denied عند أول إرسال، فستحتاج قواعد Firestore إلى السماح للمستخدم الحالي بتحديث حقل `aiUsage` في مستنده؛ لا تغيّر القواعد عشوائيًا قبل مراجعتها.
