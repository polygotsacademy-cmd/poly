# إعداد إصلاح تسجيل الدخول والجلسة

## ما الذي تغيّر؟

تم الحفاظ على قائمة المستخدمين وكلمات المرور الحالية كما هي، ولم يتم حذف أو تعديل أي ملف محتوى. التغيير يخص طريقة الجلسة فقط:

- لم يعد الموقع يحفظ كلمة المرور في `localStorage`.
- بعد تسجيل الدخول ينشئ الخادم Cookie باسم `polyglots_session`.
- الـCookie موقعة ومحمية بـ `HttpOnly` و`SameSite=Lax`.
- خيار «تذكرني» يحفظ الجلسة لمدة 30 يومًا، بينما الجلسة العادية تنتهي عند إغلاق المتصفح.
- عند فتح الموقع يحاول استعادة الجلسة من `/api/session`.
- تمت إضافة `/api/logout` وزر خروج في الواجهة.
- تم الاحتفاظ ببيانات المستخدم غير الحساسة في `polyglots_auth_data` لأن بعض الوحدات القديمة تحتاجها لعرض الصلاحيات وقائمة الطلاب.

## متغير Vercel المطلوب

في إعدادات مشروع Vercel أضف Environment Variable باسم:

```text
SESSION_SECRET
```

ولقيمة المتغير استخدم سلسلة عشوائية طويلة لا تقل عن 32 حرفًا. لا تضع هذه القيمة داخل GitHub أو داخل أي ملف JavaScript. فعّلها لكل من Preview وProduction إذا كنت تختبر المعاينات.

بعد إضافة المتغير أعد النشر.

## الاختبار بعد النشر

1. افتح الموقع في نافذة خاصة أو امسح Cookies الموقع القديمة.
2. سجّل الدخول ببيانات مستخدم صحيحة.
3. أغلق الموقع وافتحه مرة أخرى مع تفعيل «تذكرني» وتأكد من استعادة الجلسة.
4. جرّب الدخول دون تفعيل «تذكرني» وتأكد من أن الجلسة ليست دائمة بعد إغلاق المتصفح.
5. اضغط «خروج» وتأكد من عودة شاشة الدخول.
6. جرّب كلمة مرور خاطئة وتأكد من رفض الدخول.
7. راجع أن `localStorage` لا يحتوي على `polyglots_user` أو كلمة مرور.

## ملاحظة مهمة

هذه خطوة انتقالية تحافظ على كلمات المرور الحالية. في المرحلة الأمنية التالية يُفضّل نقل الحسابات إلى Firebase Authentication، لكن ذلك يمكن أن يتم دون حذف بيانات المحتوى وبعد وضع خطة ترحيل للحسابات.
